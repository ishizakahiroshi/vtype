<?xml version="1.0" encoding="utf-8"?>
<!--
  Microsoft Store MSIX package manifest for vtype desktop (child plan C9-C2).

  scripts/release/build-msix.ps1 fills the {{…}} fields:
    {{VERSION}}                 packages/native/Cargo.toml X.Y.Z as X.Y.Z.0 (the Store keeps the last field)
    {{IDENTITY_NAME}}           Partner Center: Package/Identity/Name
    {{PUBLISHER}}               Partner Center: Package/Identity/Publisher (CN=<GUID>)
    {{PUBLISHER_DISPLAY_NAME}}  Partner Center: Package/Properties/PublisherDisplayName
  The script's defaults are the values Partner Center gave for vtype (Store ID 9PJSKZSMRV57).

  Why each extra is here:
    runFullTrust               a Win32 program (the Store requires it for desktop apps)
    StartupTask                starts with Windows (parent plan D12); run without arguments, and vtype
                               without arguments starts the daemon
    AppExecutionAlias          `vtype` on the command line
  Nothing is registered with Chrome: vtype starts a Chrome of its own for speech recognition, so
  the package needs no restricted capability besides runFullTrust (standalone plan C4).
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap5="http://schemas.microsoft.com/appx/manifest/uap/windows10/5"
  xmlns:desktop="http://schemas.microsoft.com/appx/manifest/desktop/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap uap5 desktop rescap">

  <Identity
    Name="{{IDENTITY_NAME}}"
    Publisher="{{PUBLISHER}}"
    Version="{{VERSION}}"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>vtype</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY_NAME}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <!-- Windows 10 2004 or later. -->
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.19041.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
    <Resource Language="ja-jp" />
  </Resources>

  <Applications>
    <Application Id="App" Executable="vtype.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="vtype"
        Description="Voice input into any app, with Google Chrome's speech recognition"
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile Square71x71Logo="Assets\Square71x71Logo.png" />
      </uap:VisualElements>
      <Extensions>
        <desktop:Extension Category="windows.startupTask" Executable="vtype.exe" EntryPoint="Windows.FullTrustApplication">
          <desktop:StartupTask TaskId="vtypeDaemon" Enabled="true" DisplayName="vtype" />
        </desktop:Extension>
        <uap5:Extension Category="windows.appExecutionAlias" Executable="vtype.exe" EntryPoint="Windows.FullTrustApplication">
          <uap5:AppExecutionAlias>
            <uap5:ExecutionAlias Alias="vtype.exe" />
          </uap5:AppExecutionAlias>
        </uap5:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
