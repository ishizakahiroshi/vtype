<?xml version="1.0" encoding="utf-8"?>
<!--
  Microsoft Store MSIX package manifest for vtype desktop (child plan C9-C2).

  scripts/release/build-msix.ps1 fills the {{…}} fields:
    {{VERSION}}                 packages/native/Cargo.toml X.Y.Z as X.Y.Z.0 (the Store keeps the last field)
    {{IDENTITY_NAME}}           Partner Center: Package/Identity/Name            TODO: Partner Center の値
    {{PUBLISHER}}               Partner Center: Package/Identity/Publisher       TODO: Partner Center の値 (CN=<GUID>)
    {{PUBLISHER_DISPLAY_NAME}}  Partner Center: Package/Properties/PublisherDisplayName   TODO: Partner Center の値
  Until the app is reserved, the script uses placeholder values that pack and sideload but are not
  what the Store will accept.

  Why each extra is here:
    runFullTrust               a Win32 program (the Store requires it for desktop apps)
    unvirtualizedResources     with RegistryWriteVirtualization / FileSystemWriteVirtualization off,
                               the Native Messaging registration goes to the real HKCU and LocalAppData,
                               where Chrome reads it (parent plan Q6; the Store needs a reason:
                               docs/store/msstore-restricted-capability.*.md)
    StartupTask                starts with Windows (parent plan D12); run without arguments, and vtype
                               without arguments inside the package runs the daemon
    AppExecutionAlias          `vtype` on the command line, and a path for Chrome that survives updates
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap5="http://schemas.microsoft.com/appx/manifest/uap/windows10/5"
  xmlns:desktop="http://schemas.microsoft.com/appx/manifest/desktop/windows10"
  xmlns:desktop6="http://schemas.microsoft.com/appx/manifest/desktop/windows10/6"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap uap5 desktop desktop6 rescap">

  <Identity
    Name="{{IDENTITY_NAME}}"
    Publisher="{{PUBLISHER}}"
    Version="{{VERSION}}"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>vtype</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY_NAME}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <desktop6:RegistryWriteVirtualization>disabled</desktop6:RegistryWriteVirtualization>
    <desktop6:FileSystemWriteVirtualization>disabled</desktop6:FileSystemWriteVirtualization>
  </Properties>

  <Dependencies>
    <!-- Windows 10 2004 or later (the desktop6 write-virtualization switches need 1903+). -->
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.19041.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
    <Resource Language="ja-jp" />
  </Resources>

  <Applications>
    <Application Id="App" Executable="vtype.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="vtype"
        Description="Voice input into any app, with the vtype Chrome extension's speech recognition"
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile Square71x71Logo="Assets\Square71x71Logo.png" />
      </uap:VisualElements>
      <Extensions>
        <desktop:Extension Category="windows.startupTask" Executable="vtype.exe" EntryPoint="Windows.FullTrustApplication">
          <desktop:StartupTask TaskId="vtypeDaemon" Enabled="true" DisplayName="vtype" />
        </desktop:Extension>
        <uap5:Extension Category="windows.appExecutionAlias" Executable="vtype.exe" EntryPoint="Windows.FullTrustApplication">
          <uap5:AppExecutionAlias>
            <uap5:ExecutionAlias Alias="vtype.exe" />
          </uap5:AppExecutionAlias>
        </uap5:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
    <rescap:Capability Name="unvirtualizedResources" />
  </Capabilities>
</Package>
