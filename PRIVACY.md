# vtype プライバシーポリシー

最終更新日: 2026-09-22

**話した声は、音声認識のために、ブラウザの音声認識機能によって Google の音声認識サービスへ送られ、
文字になります（Chrome の音声認識の仕組みです。詳しくは下の「音声の行き先」）。**

それ以外に、vtype の開発者が利用者の情報を受け取ることはありません。個人情報を販売することも、
音声認識以外の目的で第三者へ渡すこともありません。アカウント登録もありません。

## 収集しない情報

開発者は次のいずれも受け取りません。

- 氏名、メールアドレス、その他の個人を特定できる情報
- 閲覧履歴、開いているページの URL
- ページの本文
- 入力欄に入力した内容
- 話した音声、およびその文字起こし
- Cookie、認証情報
- 利用状況の計測（アナリティクス）

vtype には計測用のコードも、開発者が管理するサーバーもありません。

## 音声の行き先

vtype 自身は音声認識を行いません。話した音声は、ブラウザに組み込まれた音声認識（Web Speech API）へ
そのまま渡します。

**Chrome では、この認識はブラウザが音声を Google の音声認識サービスへ送ることで行われます。**
vtype で録音を始めると、話した音声は Chrome によってその認識サービスへ送られ、認識された文字が
vtype へ返ってきます。音声を送るのはブラウザで、vtype はこの経路にほかの宛先を足すことも、
音声や文字を控えておくこともしません。

Google へ送られた音声の扱いは、Google（Chrome）のプライバシーポリシーに従います。話した内容を
Google の認識サービスへ渡したくない場合は、vtype を使わないでください。このことは、インストール直後に
開くマイクの許可ページにも、許可ボタンの前に書いてあります。

## 外部通信

上の音声認識を除き、vtype は開発者のサーバーを含むどのサーバーへもデータを送りません。開発者が管理する
サーバーはありません。パッケージのコードは外部から読み込むものを持たず、すべて拡張機能の中にあります。

## ブラウザ内に保存する情報

設定を `chrome.storage.sync` に保存します（Chrome にサインインしていると、同じアカウントの
ブラウザ間でブラウザ自身が同期します）。

- `trigger`: 録音を始めるきっかけ（マイクを押す / マイクにマウスを置く）
- `micDisplay`: マイクを出す範囲（画面上のすべての入力欄 / 使っている欄だけ）
- `micOffsets`: マイクをドラッグして動かした位置。サイトのオリジンごとに 1 件、最大 50 件
- `excludedSites`: vtype を動かさないサイトの一覧。最大 100 件
- `diagnostics`: 診断ログを取るかどうか（既定は取らない）
- `inputMode`: 入力モード（通常 / 英語 / カタカナ）
- `desktopBridge`: デスクトップ版との連携を有効にしているかどうか（既定は無効）

置き換え表（`replacements`。「A と認識されたら B にする」の組を利用者が自分で登録したもの）は
`chrome.storage.local` に保存します。デスクトップ版との接続状態と、その設定の写し（`desktopBridgeStatus` /
`nativeConfig`）は `chrome.storage.session` に置き、ブラウザを閉じると消えます。

`excludedSites` だけは `chrome.storage.local` にも書きます。組織の設定で同期が拒否される環境があり、
「切ったはずのサイトでマイクが戻る」ことを避けるためです。

`micOffsets` と `excludedSites` には、利用者が自分で選んだサイトのオリジン（`https://example.com`
の形）が含まれます。これらはブラウザの中にとどまり、開発者へは送信されません。

## 診断ログ

**既定では取りません。** 設定ページで「診断ログを取る」を選んだときだけ記録します。「なぜそこで録音が
止まったのか」を調べるためだけのもので、記録するのは次の情報です。

- 時刻
- 音声認識が何回やり直されたかの通し番号
- イベントの種類（開始 / 発話の検出 / 結果の到着 / 終了）
- 結果として届いた **文字数**（何文字だったか）
- 録音が終わった理由（利用者が止めた / 無音 / エラーとそのコード）

**話した内容も、認識された文字も記録しません。** 文字数だけで、文字そのものは残りません。ログは
`chrome.storage.local`（このブラウザの中）に最大 300 件まで保存され、古いものから消えます。設定ページ
からいつでも全文を表示・コピー・削除できます。**開発者へは送信されません。**

## 文字起こしの保存

保存しません。認識した文字は入力欄へ入れたらそれで終わりで、履歴も、検索も、書き出しもありません。
診断ログを取っている間も、残るのは文字数だけで、文字そのものは残りません。

## パスワード欄

パスワード欄では動きません。マイクも出しません。対象にする入力欄の種類を許可リストで決めており、
パスワードはその中にありません。読み取り専用の欄と無効化された欄も対象外です。

## 使用する権限

- `offscreen`: 拡張機能自身のページで音声認識を動かすために使います。これにより、マイクの許可は
  拡張機能に対して一度だけで済み、サイトごとに許可ダイアログが出ることがありません
- `storage`: 上記の設定を保存するために使います
- すべてのサイトでの実行（content script の `http://*/*` と `https://*/*`）: 入力欄はどのサイトにも
  あるため、マイクをどのサイトの欄にも出せる必要があります。vtype はホスト権限（`host_permissions`）も
  `tabs` 権限も要求しないので、拡張機能の本体はタブがどのサイトを開いているかを知りません
- `nativeMessaging`（任意の権限。最初は持っていません）: 設定ページで「デスクトップ連携を有効にする」を
  押し、利用者が許可したときだけ得ます。同じパソコンに入れたデスクトップ版へ認識した文字を渡すためだけに
  使い、ほかのプログラムとは通信しません

## デスクトップ版（Windows / macOS / Linux）

デスクトップ版は、拡張機能が認識した文字を、ブラウザの外のアプリへ入力する別のプログラムです。入れる
かどうかは任意で、入れなくても拡張機能はそのまま使えます。

- 音声認識はこれまでどおり Chrome が行います（上の「音声の行き先」のとおり）。デスクトップ版自身は
  マイクを使わず、音声を扱いません
- デスクトップ版はネットワークへ何も送りません。やり取りする相手は、同じパソコンの Chrome
  （Native Messaging）と、同じパソコンで動く自分自身のコマンド（`vtype toggle` など）だけです
- 認識した文字は前面のアプリへ入れたらそれで終わりで、保存しません。設定で「貼り付け方式」を選んだ
  場合と、Linux の Wayland で直接入力できない場合は、文字をクリップボードに入れます（貼り付け方式では、
  貼り付けた後に元のクリップボードの中身を戻します）
- パスワード欄には入力しません（Windows と macOS は OS に欄の種類を尋ね、Linux は OS のアクセシビリティ
  機能で判定できる範囲で除外します）
- パソコンの中に、設定ファイル（`config.json`: ショートカット・入力モード・アイコンの表示など）と
  動作ログ（`vtype.log`: 最大 1MB を 2 世代。時刻・動作の種類・エラーだけで、話した内容や文字そのものは
  記録しません）を置きます。開発者へは送信されません

## 不具合の報告

拡張機能の設定ページとデスクトップ版のメニューにある「不具合を報告」は、GitHub の新しい issue の
入力画面を、版と OS とブラウザの名前を埋めた状態でブラウザに開くだけです。自動では何も送りません。
送るかどうかは、利用者が画面の内容を見て決めます。デスクトップ版のメニューの「診断情報をコピー」は、
OS・版・設定・直近のエラーをクリップボードへ写すだけで、
音声と文字起こしの本文を含みません。

## 変更

このポリシーを変更する場合は、この文書の更新によって通知します。

## 連絡先

https://github.com/ishizakahiroshi/vtype/issues

---

English version: [docs/store/privacy-policy.en.md](docs/store/privacy-policy.en.md)

This file is a copy of [docs/store/privacy-policy.ja.md](docs/store/privacy-policy.ja.md), which is the source. `scripts/validate-extension.ps1` fails if the two drift apart.
